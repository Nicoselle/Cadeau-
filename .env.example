# Copy to .env.local and fill in your key.

# Required at runtime to generate reports (not needed for `npm run build`).
ANTHROPIC_API_KEY=

# Optional: override the model used for validation. Defaults to claude-opus-4-8.
# ANTHROPIC_MODEL=claude-opus-4-8
